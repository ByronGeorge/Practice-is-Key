#include <iostream>
#include <cstdio>
using namespace std;

/*
int ("%d"): 32 Bit integer
Long ("%ld"): 32 bit integer (same as Int for modern systems)
Long Long ("%lld"): 64 bit integer
Char ("%c"): Character type
Float ("%f"): 32 bit real value
Double ("%lf"): 64 bit real value
*/

int main() {
    int d;
    long ld;
    long long lld;
    char ch;
    float f;
    double lf;
    scanf("%d %ld %lld %c %f %lf", &d, &ld, &lld, &ch, &f, &lf);
    printf("%d \n%ld \n%lld \n%c \n%f \n%lf", d, ld, lld, ch, f, lf);
    return 0;
}
